package org.frogforce503.robot.commands;

import edu.wpi.first.wpilibj2.command.Command;

public class IntakeFuelFromGround extends Command { // involves some sort of fetching (no need of a separate fetching cmd, just needs to natural enough for the good driver, see orbit intake assist)
    
}
